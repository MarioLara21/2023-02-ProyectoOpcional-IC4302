apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "Stateful.fullname" . }}-test-connection"
  labels:
    {{- include "Stateful.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "Stateful.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
